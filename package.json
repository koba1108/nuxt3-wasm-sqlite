{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "deploy": "gh-pages -d dist"
  },
  "devDependencies": {
    "@types/sql.js": "^1.4.4",
    "gh-pages":"^4.0.0",
    "nuxt": "3.0.0"
  },
  "dependencies": {
    "sql.js": "^1.8.0"
  }
}
